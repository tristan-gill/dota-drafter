{
  "name": "dotagsi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "heroku-postbuild": "npm run build && node scripts/sync-public.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "events": "^3.3.0",
    "express": "^4.18.2",
    "path": "^0.12.7",
    "socket.io": "^4.5.3"
  }
}
